# Decision log

One file per gameweek: the card as issued, what the optimiser proposed, what
was overridden and why, and what was considered and rejected.

Next season's edge is in here. Write it as if someone else has to audit it.
